1. Each runs.sh may contain several cases, and each case may consume considerable memory. If the process is killed due to the memory limit, we should run each case separately.

2. All settings for plotting figures are in set_config.py. One should specify the setting and run it first before running other script, i.e., FCT.py, BE_BAR.py.